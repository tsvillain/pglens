# Presence of this file makes npm ignore client-next/.gitignore for packing.
# The root package.json "files" allowlist restricts the published set to
# client-next/dist, so nothing else here is shipped.
